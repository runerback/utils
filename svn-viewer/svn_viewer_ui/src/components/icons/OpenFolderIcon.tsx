import OpenFolderHorizontal from "../../assets/OpenFolderHorizontal.svg?react";

export default (props: { className?: string }) => (
  <OpenFolderHorizontal className={props.className ?? "icon"} />
);
